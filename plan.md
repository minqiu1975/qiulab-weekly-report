# 新增成员 + 周报自动发现新成员

## 1. SettingsPage - 新增成员功能
- 在团队成员表格上方添加"新增成员"按钮
- 点击后展开表单（姓名、角色、研究方向、入学年份等）
- 自动生成ID
- 保存到 localStorage + 云端同步

## 2. ReportUploader - 周报自动发现新成员
- 从 docx 文本中提取人名（基于周报格式特征）
- 与现有成员列表比对，找出不在列表中的人名
- 弹出提示框，列出发现的新成员
- 用户确认后自动添加（默认类别为当年入职的博士生）
- 保存到 localStorage + 云端同步
